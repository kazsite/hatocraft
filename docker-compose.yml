services:
  mc:
    image: itzg/minecraft-server
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      MEMORY: 2G
      DIFFICULTY: normal
      MAX_PLAYERS: 25
      MODE: survival
    volumes:
      - ./data:/data